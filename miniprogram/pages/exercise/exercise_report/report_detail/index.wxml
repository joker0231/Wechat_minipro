<!-- 大部分抄袭exercise_online，不同之处：
1. 用户不能选选择题 输入思路
2. 展示答案
3. 展示用户输入（页面传值） exercise_oline exercise_report和report_detail用westore处理传值 
比如逻辑：这个页面的current也可以用全局传递 上一个页面onclick设置curretn这里用到 就很强
4. 右上角答题变成了 发帖 在线聊天 收藏三大件 （这里收藏是收藏一道道题目 历史记录是一个个做题报告） -->
<view class="exercise_header">
  <view class="show_index">{{current + 1 + '/' + exercise.length}}</view>
  <view class="title-first">{{section}}{{title}}</view>
  <gl_navigate></gl_navigate>
</view>
<view class="exercise_content"> 
  <swiper class="swiper" current="{{current}}" bindchange="onChange">
    <swiper-item class="swiper-item" wx:for="{{exercise}}">
      <view class="card">
        <scroll-view class="scroll" scroll-y="true">
          <view class="title">
          <rich-text nodes="{{item.question}}"></rich-text>
          </view>

          <!-- 选择题情况 -->
          <view class="choice" wx:if="{{item.type === 'choice'}}">
            <view class="small-choice" wx:for="{{item.options}}">
              <view wx:if="{{user_input[current] !== index}}" data-choiceIndex="{{index}}" class="small-choice-text">{{item}}</view>
              <view wx:else style="background-color: orange;color: white;" data-choiceIndex="{{index}}" class="small-choice-text">{{item}}</view>
            </view>
            <view wx:if="{{user_input[current] !== index}}" style="font-size: 28rpx; color: #b4ebf1">您没有选择答案</view>
          </view>

          <!-- 简答题情况 -->
          <view class="text" wx:if="{{item.type === 'text'}}">
            记录思路：
            <view class="field">
              <van-field
                class="field"
                value="{{ user_input[current] === '' ? '没有填写思路！' : user_input[current] }}"
                type="textarea"
                placeholder="您没有简答思路"
                autosize
                wx:if="{{user_input[current] !== null}}"
                border="{{ false }}"
                readonly
              />
              <view wx:else style="color: #b4ebf1;">您没有简答思路</view>
            </view>
          </view>
          <view class="text">
            参考答案：
            <view class="field">
              <view class="answer">{{item.solution}}</view>
            </view>
          </view>
          
          
        </scroll-view>

        <view class="two-buttons">
          <view wx:if="{{current !== 0}}" bindtap="onClickPrev">上一题</view>
          <view wx:if="{{current !== exercise.length-1}}" class="second-button" bindtap="onClickNext">下一题</view>
        </view>
      </view>
    </swiper-item>
  </swiper>
</view>